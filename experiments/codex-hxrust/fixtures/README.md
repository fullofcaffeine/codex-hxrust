# Fixtures

Local copied fixtures go here only after a gate selects them.

Do not copy whole upstream trees. Keep source pins and paths in manifests.

Subdirectories:

- `upstream/`: upstream Codex fixture subset for M2-M4
- `cafex/`: Cafex/Cafetera adapter fixture subset for M5
- `hxrust/`: haxe.rust capability or compiler smoke fixtures
